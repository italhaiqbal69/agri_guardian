import 'package:flutter/cupertino.dart';

class ReuasbleContainer extends StatelessWidget {
  const ReuasbleContainer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}